server:
  port: 8080
spring:
  application:
    name: client
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/client
    username: Farzan
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
